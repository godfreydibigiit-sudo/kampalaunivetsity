"""
URL patterns for academics app
"""
from django.urls import path
from . import views

app_name = 'academics'

urlpatterns = [
    # Most operations handled by Django Admin
    # Add custom views here if needed in future
]